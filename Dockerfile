# Використовуємо базовий образ Python
FROM python:3.11

# Встановлюємо необхідні пакети
RUN pip install flask

# Копіюємо файли додатку до контейнера
COPY . /app
WORKDIR /app

# Вказуємо, на якому порту працює додаток
EXPOSE 5000

# Команда для запуску додатку при старті контейнера
CMD ["python", "main.py"]
